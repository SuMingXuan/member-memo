<div class="flex justify-end sticky top-[0px] z-[100] shadow left-0 p-[20px] mx-[-20px] mt-[-20px] lg:m-0 lg:shadow lg:p-[20px] bg-white text-primary-599">
  <%= link_to '退出登录', destroy_user_session_path, data: {turbo_method: :delete} %>
</div>
<div class="p-[20px] pb-0 lg:p-[40px]">
  <%= react_component "product/List",
    user: current_user.as_json_list,
    products: @products.map(&:as_json_list),
    invitation_url: user_session_url(invitation_code: current_user.code),
    alipay_icon: image_path('alipay.svg'),
    wxpay_icon: image_path('wxpay.svg')
    %>
</div>
